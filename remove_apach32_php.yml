---
- hosts: all
  become: true
  tasks:

    - name: Update Repository
      apt:
        update_cache: yes

    - name: Remove Apache2 Package
      apt:
        name: apache2
        state: absent

    - name: Remove NGINX
      apt:
        name: nginx
        state: absent
